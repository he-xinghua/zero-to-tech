# 我的交互小工具

一个轻量级的前端练习页面，包含深色模式、实时时钟、问候语切换和计数器等交互功能。

## 功能

- **深色模式** — 一键切换亮/暗主题，自动保存偏好
- **实时时钟** — 页面顶部显示当前时间，每秒刷新
- **智能问候** — 根据当前时段显示不同的问候语
- **消息切换** — 点击按钮轮换多条励志文案
- **计数器** — 支持加减和重置操作

## 技术栈

- 原生 HTML5 + CSS3 + JavaScript
- CSS 自定义属性实现主题切换
- `localStorage` 持久化用户偏好
